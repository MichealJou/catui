import { defineComponent, computed } from "vue";
import { i18n, S2_PREFIX_CLS } from "@antv/s2";
import { Pagination } from "ant-design-vue";
import { DEFAULT_PAGE_SIZE, DEFAULT_PAGE_NUMBER } from "../../hooks/usePagination.js";
const PRE_CLASS = `${S2_PREFIX_CLS}-pagination`;
const _sfc_main = defineComponent({
  name: "s2-pagination",
  props: {
    current: {
      type: Number,
      default: DEFAULT_PAGE_NUMBER
    },
    total: {
      type: Number,
      default: 0
    },
    pageSize: {
      type: Number,
      default: DEFAULT_PAGE_SIZE
    },
    customOptions: Object
  },
  emits: ["change", "showSizeChange"],
  setup: (props) => {
    const showQuickJumper = computed(() => props.total / props.pageSize > 5);
    return {
      showQuickJumper,
      PRE_CLASS,
      i18n
    };
  },
  components: {
    AntDPagination: Pagination
  }
});
export {
  _sfc_main as default
};
//# sourceMappingURL=index.vue2.js.map
