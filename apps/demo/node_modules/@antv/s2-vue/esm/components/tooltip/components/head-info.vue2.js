import { i18n, TOOLTIP_PREFIX_CLS } from "@antv/s2";
import { defineComponent, computed } from "vue";
const _sfc_main = defineComponent({
  name: "TooltipHeadInfo",
  props: ["rows", "cols"],
  setup(props) {
    const colsText = computed(
      () => (props.cols || []).map((item) => item.value).join("/")
    );
    const rowsText = computed(
      () => (props.rows || []).map((item) => item.value).join("/")
    );
    const separator = computed(
      () => {
        var _a, _b, _c, _d;
        return ((_b = (_a = props.cols) == null ? void 0 : _a.length) != null ? _b : 0) > 0 && ((_d = (_c = props.rows) == null ? void 0 : _c.length) != null ? _d : 0) > 0 ? i18n("，") : "";
      }
    );
    return {
      TOOLTIP_PREFIX_CLS,
      colsText,
      rowsText,
      separator
    };
  }
});
export {
  _sfc_main as default
};
//# sourceMappingURL=head-info.vue2.js.map
