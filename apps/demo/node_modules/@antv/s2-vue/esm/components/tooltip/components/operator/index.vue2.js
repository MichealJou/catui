import { TOOLTIP_PREFIX_CLS } from "@antv/s2";
import { Dropdown, Menu } from "ant-design-vue";
import { defineComponent } from "vue";
import TooltipOperatorTitle from "./title.vue2.js";
import TooltipOperatorMenu from "./menu.vue2.js";
const _sfc_main = defineComponent({
  name: "TooltipOperator",
  props: [
    "menu",
    "onlyShowOperator",
    "cell"
  ],
  setup(props) {
    const { menu, cell } = props;
    const onMenuClick = (menuInfo) => {
      var _a;
      (_a = menu == null ? void 0 : menu.onClick) == null ? void 0 : _a.call(menu, menuInfo, cell);
    };
    const onMenuTitleClick = (subMenu) => () => {
      subMenu == null ? void 0 : subMenu.onClick(subMenu, cell);
    };
    return {
      TOOLTIP_PREFIX_CLS,
      onMenuClick,
      onMenuTitleClick,
      menus: menu == null ? void 0 : menu.items
    };
  },
  components: {
    Menu,
    Dropdown,
    TooltipOperatorTitle,
    TooltipOperatorMenu
  }
});
export {
  _sfc_main as default
};
//# sourceMappingURL=index.vue2.js.map
