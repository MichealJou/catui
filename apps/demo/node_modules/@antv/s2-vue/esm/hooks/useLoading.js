import { S2Event } from "@antv/s2";
import { ref, watch } from "vue";
const useLoading = (s2Ref, loadingProp) => {
  var _a;
  const loading = ref((_a = loadingProp.value) != null ? _a : false);
  const setLoading = (updated) => {
    loading.value = updated;
  };
  watch(loadingProp, (newLoading) => {
    loading.value = newLoading != null ? newLoading : false;
  });
  watch(s2Ref, (s2) => {
    s2 == null ? void 0 : s2.on(S2Event.LAYOUT_BEFORE_RENDER, () => {
      setLoading(true);
    });
    s2 == null ? void 0 : s2.on(S2Event.LAYOUT_AFTER_RENDER, () => {
      setLoading(false);
    });
  });
  return { loading, setLoading };
};
export {
  useLoading
};
//# sourceMappingURL=useLoading.js.map
