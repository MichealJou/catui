"use strict";
const s2 = require("@antv/s2");
const vue = require("vue");
const _sfc_main = vue.defineComponent({
  name: "TooltipIcon",
  props: {
    icon: {
      type: [String, Object],
      default: void 0
    },
    width: {
      type: Number,
      default: void 0
    },
    height: {
      type: Number,
      default: void 0
    }
  },
  setup(props) {
    const svgIcon = vue.computed(() => {
      if (typeof props.icon === "string") {
        return s2.getIcon(props.icon);
      }
      return null;
    });
    const iconStyle = vue.computed(() => {
      const style = {};
      if (props.width) {
        style["width"] = `${props.width}px`;
      }
      if (props.height) {
        style["height"] = `${props.height}px`;
      }
      return style;
    });
    const isStringIcon = vue.computed(() => {
      return typeof props.icon === "string" && s2.getIcon(props.icon);
    });
    return {
      svgIcon,
      iconStyle,
      isStringIcon,
      TOOLTIP_PREFIX_CLS: s2.TOOLTIP_PREFIX_CLS
    };
  }
});
module.exports = _sfc_main;
//# sourceMappingURL=icon.vue2.js.map
