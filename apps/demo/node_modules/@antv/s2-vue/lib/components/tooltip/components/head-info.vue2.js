"use strict";
const s2 = require("@antv/s2");
const vue = require("vue");
const _sfc_main = vue.defineComponent({
  name: "TooltipHeadInfo",
  props: ["rows", "cols"],
  setup(props) {
    const colsText = vue.computed(
      () => (props.cols || []).map((item) => item.value).join("/")
    );
    const rowsText = vue.computed(
      () => (props.rows || []).map((item) => item.value).join("/")
    );
    const separator = vue.computed(
      () => {
        var _a, _b, _c, _d;
        return ((_b = (_a = props.cols) == null ? void 0 : _a.length) != null ? _b : 0) > 0 && ((_d = (_c = props.rows) == null ? void 0 : _c.length) != null ? _d : 0) > 0 ? s2.i18n("，") : "";
      }
    );
    return {
      TOOLTIP_PREFIX_CLS: s2.TOOLTIP_PREFIX_CLS,
      colsText,
      rowsText,
      separator
    };
  }
});
module.exports = _sfc_main;
//# sourceMappingURL=head-info.vue2.js.map
