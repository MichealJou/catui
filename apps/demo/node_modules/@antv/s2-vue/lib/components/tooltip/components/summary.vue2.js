"use strict";
const lodash = require("lodash");
const s2 = require("@antv/s2");
const vue = require("vue");
const _sfc_main = vue.defineComponent({
  name: "TooltipSummary",
  props: ["summaries"],
  setup(props) {
    const summaryInfo = vue.computed(
      () => lodash.reduce(
        props.summaries,
        (pre, next) => {
          pre.count += lodash.size(next == null ? void 0 : next.selectedData);
          if (next.value || next.name) {
            pre.summaries.push(next);
          }
          return pre;
        },
        { count: 0, summaries: [] }
      )
    );
    return {
      summaryInfo,
      i18n: s2.i18n,
      TOOLTIP_PREFIX_CLS: s2.TOOLTIP_PREFIX_CLS
    };
  },
  components: {}
});
module.exports = _sfc_main;
//# sourceMappingURL=summary.vue2.js.map
