"use strict";
const vue = require("vue");
const s2 = require("@antv/s2");
const antDesignVue = require("ant-design-vue");
const usePagination = require("../../hooks/usePagination.js");
const PRE_CLASS = `${s2.S2_PREFIX_CLS}-pagination`;
const _sfc_main = vue.defineComponent({
  name: "s2-pagination",
  props: {
    current: {
      type: Number,
      default: usePagination.DEFAULT_PAGE_NUMBER
    },
    total: {
      type: Number,
      default: 0
    },
    pageSize: {
      type: Number,
      default: usePagination.DEFAULT_PAGE_SIZE
    },
    customOptions: Object
  },
  emits: ["change", "showSizeChange"],
  setup: (props) => {
    const showQuickJumper = vue.computed(() => props.total / props.pageSize > 5);
    return {
      showQuickJumper,
      PRE_CLASS,
      i18n: s2.i18n
    };
  },
  components: {
    AntDPagination: antDesignVue.Pagination
  }
});
module.exports = _sfc_main;
//# sourceMappingURL=index.vue2.js.map
