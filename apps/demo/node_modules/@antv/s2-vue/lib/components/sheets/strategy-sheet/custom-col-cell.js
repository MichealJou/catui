"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const s2 = require("@antv/s2");
const lodash = require("lodash");
class StrategySheetColCell extends s2.ColCell {
  constructor(meta, spreadsheet, headerConfig) {
    super(meta, spreadsheet, headerConfig);
  }
  drawTextShape() {
    const meta = this.getMeta();
    const value = s2.safeJsonParse(meta == null ? void 0 : meta.value);
    if (!lodash.isArray(value)) {
      return super.drawTextShape();
    }
    const { formattedValue } = this.getFormattedFieldValue();
    const displayValues = formattedValue !== (meta == null ? void 0 : meta.value) ? [[formattedValue]] : [value];
    s2.drawCustomContent(
      this,
      {
        values: displayValues
      },
      false
    );
  }
}
exports.StrategySheetColCell = StrategySheetColCell;
//# sourceMappingURL=custom-col-cell.js.map
